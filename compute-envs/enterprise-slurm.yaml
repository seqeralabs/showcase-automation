compute-envs:
  - ref: slurm
    name: seqera_slurm
    workdir: /home/seqera/work
    workspace: "80631102134985"
    profiles: []  # Default profiles for pipelines without specific mappings
    profile_mappings:
      # Apply singularity profile to nf-core pipelines
      - pipelines: ["nf-core-*", "rnaseq", "sarek", "viralrecon-*"]
        profiles: ["singularity"]
      # Hello pipeline should not use singularity profile
      - pipelines: ["hello"]
        profiles: []
